version: 0.2
phases:
  install:
    commands:
      - npm ci
      - echo installed dependencies
      - npx org-formation -v
  build:
    commands:
      - npx org-formation perform-tasks ./src/_tasks.yml --no-color --max-concurrent-stacks 50 --max-concurrent-tasks 1
  post_build:
    commands:
      - echo Build completed
