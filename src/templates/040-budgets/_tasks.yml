Parameters:
  <<: !Include "../../_parameters.yml"

Budgets:
  Type: update-stacks
  Template: ./budgets.yml
  StackName: !Sub "${resourcePrefix}-budgets"
  DefaultOrganizationBinding:
    AccountsWithTag: service-quotas-s3-buckets
    Region: !Ref primaryRegion